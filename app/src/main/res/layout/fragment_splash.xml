<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:padding="24dp">

    <!-- Top Card / Branding Section -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/brandCard"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:cardCornerRadius="28dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="@color/sleek_purple_container"
        app:strokeWidth="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/guideline"
        android:layout_marginBottom="24dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="32dp">

            <!-- App Logo Icon Frame -->
            <FrameLayout
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:background="@drawable/ic_launcher_background"
                android:backgroundTint="@color/primary"
                android:layout_marginBottom="20dp"
                android:elevation="4dp"
                android:clipToOutline="true">

                <ImageView
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_room"
                    app:tint="@color/white"
                    android:contentDescription="Hostel Logo" />
            </FrameLayout>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hostel Manager Pro"
                android:textSize="28sp"
                android:textStyle="bold"
                android:textColor="@color/sleek_purple_on_container"
                android:layout_marginBottom="8dp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Enterprise Residential Operations"
                android:textSize="14sp"
                android:textColor="@color/sleek_purple_on_container"
                android:alpha="0.8"
                android:gravity="center" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.55" />

    <!-- Role Selection Title -->
    <TextView
        android:id="@+id/selectRoleTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Your Portal"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/on_background"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@+id/guideline"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Admin Portal Choice -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_admin_portal"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Administrator Portal"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/white"
        app:icon="@drawable/ic_dashboard"
        app:iconTint="@color/white"
        app:iconGravity="textStart"
        app:iconSize="24dp"
        app:cornerRadius="16dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/selectRoleTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Student Portal Choice -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_student_portal"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Student Portal"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="?attr/colorPrimary"
        app:icon="@drawable/ic_student"
        app:iconTint="?attr/colorPrimary"
        app:iconGravity="textStart"
        app:iconSize="24dp"
        app:cornerRadius="16dp"
        style="@style/Widget.Material3.Button.OutlinedButton"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/btn_admin_portal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Footer Copyright -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="v1.0.0 • SQLite Offline Native System"
        android:textSize="12sp"
        android:textColor="@color/grey_600"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="8dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
